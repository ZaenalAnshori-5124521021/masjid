
  # Sidebar Navigation Design (Copy)

  This is a code bundle for Sidebar Navigation Design (Copy). The original project is available at https://www.figma.com/design/kHTkWKbZrta67CvHuUauUm/Sidebar-Navigation-Design--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  